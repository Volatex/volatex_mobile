<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ru.volatex.volatexmobile">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VolatexMobile"
        tools:targetApi="31">
        <activity android:name=".presentation.LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".presentation.verify.VerifyEmailActivity" />
        <activity android:name=".presentation.login.LoginActivity" />
        <activity android:name=".presentation.register.RegisterActivity" />
        <activity android:name=".presentation.main.MainActivity" />
        <activity android:name=".presentation.token.AddTokenActivity"/>

    </application>

</manifest>
